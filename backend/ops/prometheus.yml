global:
  scrape_interval: 5s
  evaluation_interval: 30s

rule_files:
  - "/etc/prometheus/alerts.yml"

scrape_configs:
  - job_name: "guardrail-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]

